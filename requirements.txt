networkx>=3.0
ultralytics>=8.0
supervision>=0.20
fastapi==0.137.0
uvicorn[standard]==0.49.0
websockets==16.0
psutil>=6.0.0
mlx-vlm>=0.1.0
Pillow>=10.0.0
beautifulsoup4>=4.12.0
httpx>=0.27.0

# file_processor
pdfplumber>=0.11.0
pandas>=2.0.0
openpyxl>=3.1.0
python-docx>=1.1.0
# openai-whisper  # opzionale: ~150MB, installa manualmente se serve audio

# FASE 8C — opzionali (installa manualmente se usi queste skill)
# pip install playwright && playwright install chromium   → browser_control
# pip install pyautogui                                  → computer_control
